# skk-hub

The pinned repositories are the public parts of my work: a commit-time validator for a markdown
vault, a Node.js integration tool, and a gated content pipeline built in a timed hiring assessment.
The MCP services and the retrieval system they came out of are private.

Claude Code and Codex produce much of the implementation; I define the behaviour and acceptance
criteria, review what they produce against those, and own testing and deployment.
